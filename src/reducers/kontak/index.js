const  initialState = {
    
}

const kontak = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
            break;
    }
}

export default kontak;